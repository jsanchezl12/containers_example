# Indica la imagen base que se utilizará para construir esta imagen
FROM nginx:latest

# Establece el directorio de trabajo
WORKDIR /usr/share/nginx/html

# Copia el archivo para la página de inicio
COPY index.html .

# Expone el puerto 80 de la imagen
EXPOSE 80

